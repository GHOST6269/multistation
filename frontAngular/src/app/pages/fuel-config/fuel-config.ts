import{ChangeDetectorRef,Component,OnInit}from'@angular/core';import{FormBuilder,Validators}from'@angular/forms';import{ArticleService}from'../../services/article.service';import{FuelService}from'../../services/fuel.service';import{DropdownOption}from'../../shared/dropdown/dropdown';
@Component({selector:'app-fuel-config',standalone:false,templateUrl:'./fuel-config.html',styleUrl:'./fuel-config.scss'})export class FuelConfig implements OnInit{stations:DropdownOption[]=[];stationId=0;data:any;tab='fuel';editing:any=null;form;constructor(private service:FuelService,private articles:ArticleService,fb:FormBuilder,private cdr:ChangeDetectorRef){this.form=fb.group({code:['',Validators.required],name:['',Validators.required],fuelTypeId:[0],capacity:[0],minimumStock:[0],pumpId:[0],tankId:[0],currentIndex:[0],unitPrice:[0],contact:['']})}ngOnInit(){this.articles.options().subscribe(x=>{this.stations=x.stations.map(s=>({value:s.id,label:s.name}));this.stationId=x.stations[0]?.id??0;this.load()})}load(){this.service.config(this.stationId).subscribe(x=>{this.data=x;this.cdr.detectChanges()})}get items(){return this.data?.[`${this.tab}s`]??[]}options(items:any[]):DropdownOption[]{return(items??[]).filter(x=>x.active).map(x=>({value:x.id,label:x.name,hint:x.code??''}))}edit(x:any){this.editing=x;this.form.patchValue({code:x.code,name:x.name,fuelTypeId:x.fuelTypeId??0,capacity:x.capacity??0,minimumStock:x.minimum??0,pumpId:x.pumpId??0,tankId:x.tankId??0,currentIndex:x.currentIndex??0,unitPrice:x.unitPrice??0,contact:x.contact??''})}save(){if(!this.editing||this.form.invalid)return;this.service.update(this.tab,this.editing.id,this.form.getRawValue()).subscribe(()=>{this.editing=null;this.load()})}deactivate(x:any){if(confirm(`Désactiver ${x.name||x.code} ?`))this.service.deactivate(this.tab,x.id).subscribe(()=>this.load())}}
